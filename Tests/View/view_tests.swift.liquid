//
//  {{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ developer.company }} {{ year }}. All rights reserved.
//

@testable import {{ module_info.project_name }}
import XCTest

class {{ module_info.name }}ViewControllerTests: XCTestCase {
    // MARK: Subject under test

    var sut: {{ module_info.name }}ViewController!
    var window: UIWindow!

    // MARK: Test lifecycle

    override func setUp() {
    super.setUp()
        window = UIWindow()
        setup{{ module_info.name }}ViewController()
    }

    override func tearDown() {
        window = nil
        super.tearDown()
    }

    // MARK: Test setup

    func setup{{ module_info.name }}ViewController() {
        let bundle = Bundle.main
        let storyboard = UIStoryboard(name: "{{ module_info.name }}", bundle: bundle)
        sut = storyboard.instantiateViewController(withIdentifier: "{{ module_info.name }}ViewController") as! {{ module_info.name }}ViewController
    }

    func loadView() {
        window.addSubview(sut.view)
        RunLoop.current.run(until: Date())
    }

    // MARK: Tests
}
